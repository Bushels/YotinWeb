{
  "name": "yotin-web",
  "private": true,
  "version": "2.0.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview --port 4173 --strictPort",
    "test": "node --test test/*.test.js",
    "build:manifest": "node scripts/manifest.mjs dist",
    "check:budget": "node scripts/budget.mjs",
    "check:scroll": "node scripts/scroll-length.mjs --strict",
    "check:rm": "node scripts/rm-smoke.mjs",
    "capture:interactions": "node scripts/interactions.mjs --url http://localhost:5174/?world=1 --out scratch/frames --w 1366 --h 768",
    "check:runtime": "node scripts/frames.mjs --url http://localhost:5174/?world=1 --out scratch/frames --w 1440 --h 900 --strict --quiet && node scripts/frames.mjs --url http://localhost:5174/?world=1 --out scratch/frames --w 390 --h 844 --strict --quiet",
    "check:colour": "node scripts/colour-gate.mjs --strict",
    "check": "npm run build && node scripts/budget.mjs && node scripts/colour-gate.mjs --strict && node --test test/*.test.js",
    "check:all": "npm run check && npm run check:rm && npm run check:scroll && npm run check:runtime"
  },
  "devDependencies": {
    "playwright": "^1.60.0",
    "sharp": "^0.35.3",
    "three": "0.185.1",
    "vite": "8.2.1"
  }
}
